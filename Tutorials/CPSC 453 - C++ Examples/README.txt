To run open terminal in this directory and enter the comments

//Move to directory example
cd ./<FOLDER NAME>

// Compile all files in directory to an executable (a.exe or a.out)
g++ *.cpp *.h

// Run
./a.out

